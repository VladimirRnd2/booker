<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zuzex.booker.mapper.UserBookMapper">
    <insert id="addBookToUser">
        insert into users_books (user_id, book_id) values(#{userId}, #{bookId});
    </insert>
</mapper>